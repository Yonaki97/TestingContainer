services:

  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"

    volumes:
      - ./Frontend:/app
      - /app/node_modules

  backend:
    build: ./Backend
    ports:
      - "5000:5000"

    volumes:
      - ./Backend:/app
      - /app/node_modules